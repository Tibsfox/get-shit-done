---
phase: 05-workflow-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/verify-work.md
  - commands/gsd/debug.md
  - commands/gsd/discuss-phase.md
  - commands/gsd/research-phase.md
  - commands/gsd/map-codebase.md
  - commands/gsd/resume-work.md
  - commands/gsd/complete-milestone.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All 7 workflow files use hybrid format (prose + logger.X() code examples)"
    - "Every logging event has 'Example code:' section with logger.info() or logger.debug() call"
    - "Format matches Phase 3 agent logging standard established in execute-phase.md"
  artifacts:
    - path: "commands/gsd/verify-work.md"
      provides: "UAT session logging specs with logger.X() examples"
      contains: "logger.info\\("
    - path: "commands/gsd/debug.md"
      provides: "Debug session logging specs with logger.X() examples"
      contains: "logger.info\\("
    - path: "commands/gsd/discuss-phase.md"
      provides: "Context gathering logging specs with logger.X() examples"
      contains: "logger.info\\("
    - path: "commands/gsd/research-phase.md"
      provides: "Research workflow logging specs with logger.X() examples"
      contains: "logger.info\\("
    - path: "commands/gsd/map-codebase.md"
      provides: "Parallel mapping logging specs with logger.X() examples"
      contains: "logger.info\\("
    - path: "commands/gsd/resume-work.md"
      provides: "Session resume logging specs with logger.X() examples"
      contains: "logger.info\\("
    - path: "commands/gsd/complete-milestone.md"
      provides: "Milestone completion logging specs with logger.X() examples"
      contains: "logger.info\\("
  key_links:
    - from: "All 7 workflow files"
      to: "lib/logger.js"
      via: "logger.info()/logger.debug() calls in Example code sections"
      pattern: "logger\\.(info|debug)\\("
---

<objective>
Fix logging format inconsistency in 7 workflow orchestrator files from plan 05-02. Add `logger.X()` wrapper examples to all logging event specifications to match Phase 3 hybrid format.

Purpose: Complete the logging specification format so orchestrators know HOW to log (API call), not just WHAT to log (context objects). Without wrapper examples, the specs are incomplete.

Output: 7 workflow files with full hybrid format logging specs (prose + message format + context fields + logger.X() code example)
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-workflow-integration/05-VERIFICATION.md

# Reference for correct format
@commands/gsd/execute-phase.md (lines 330-500 - logging section with correct hybrid format)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix verify-work.md and debug.md logging format (12 events)</name>
  <files>commands/gsd/verify-work.md, commands/gsd/debug.md</files>
  <action>
Update the `<logging>` section in both files to match the Phase 3 hybrid format established in execute-phase.md.

For each logging event, transform from current format:

```markdown
### N. Event Name

**Level:** INFO (3)
**When:** timing description
**Purpose:** purpose description

**Message Format:**
Event message

**Context:**
{
  event: "category.event_name",
  field1: value1,
  ...
}
```

To hybrid format:

```markdown
### N. Event Name (INFO level)

Description combining When and Purpose into prose.

**Message format:** "Event message"

**Context to include:**
- field1: description
- field2: description

**Example code:**

logger.info('Event message', {
  field1: value1,
  field2: value2
});
```

**verify-work.md events to fix (7):**
1. UAT Session Start (INFO) - logger.info()
2. Test Present (DEBUG) - logger.debug()
3. Test Result (INFO) - logger.info()
4. UAT Checkpoint (DEBUG) - logger.debug()
5. Debug Agent Spawn (DEBUG) - logger.debug()
6. Planner Agent Spawn (DEBUG) - logger.debug()
7. UAT Session Complete (INFO) - logger.info()

**debug.md events to fix (5):**
1. Debug Session Start (INFO) - logger.info()
2. Symptom Gathering (DEBUG) - logger.debug()
3. Debugger Spawn (DEBUG) - logger.debug()
4. Checkpoint Handling (INFO) - logger.info()
5. Investigation Outcome (INFO) - logger.info()

Keep the same event names, levels, and context fields - only change the FORMAT to include logger.X() wrapper in Example code section.
  </action>
  <verify>
grep -c "logger\\.info\\|logger\\.debug" commands/gsd/verify-work.md  # Should be >= 7
grep -c "logger\\.info\\|logger\\.debug" commands/gsd/debug.md  # Should be >= 5
grep -c "Example code:" commands/gsd/verify-work.md  # Should be 7
grep -c "Example code:" commands/gsd/debug.md  # Should be 5
  </verify>
  <done>
verify-work.md has 7 logging events with logger.X() example code sections.
debug.md has 5 logging events with logger.X() example code sections.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix medium complexity orchestrator logging format (10 events)</name>
  <files>commands/gsd/discuss-phase.md, commands/gsd/research-phase.md, commands/gsd/map-codebase.md</files>
  <action>
Update the `<logging>` section in all three files to match the Phase 3 hybrid format.

Apply the same format transformation as Task 1.

**discuss-phase.md events to fix (3):**
1. Discussion Start (INFO) - logger.info()
2. Area Deep-Dive (DEBUG) - logger.debug()
3. Context Creation (INFO) - logger.info()

**research-phase.md events to fix (3):**
1. Research Start (INFO) - logger.info()
2. Researcher Spawn (DEBUG) - logger.debug()
3. Research Complete (INFO) - logger.info()

**map-codebase.md events to fix (4):**
1. Mapping Start (INFO) - logger.info()
2. Mapper Spawn (DEBUG) - logger.debug()
3. Mapper Complete (DEBUG) - logger.debug()
4. Mapping Complete (INFO) - logger.info()

Keep the same event names, levels, and context fields - only change the FORMAT to include logger.X() wrapper.
  </action>
  <verify>
grep -c "logger\\.info\\|logger\\.debug" commands/gsd/discuss-phase.md  # Should be >= 3
grep -c "logger\\.info\\|logger\\.debug" commands/gsd/research-phase.md  # Should be >= 3
grep -c "logger\\.info\\|logger\\.debug" commands/gsd/map-codebase.md  # Should be >= 4
grep -c "Example code:" commands/gsd/discuss-phase.md  # Should be 3
grep -c "Example code:" commands/gsd/research-phase.md  # Should be 3
grep -c "Example code:" commands/gsd/map-codebase.md  # Should be 4
  </verify>
  <done>
discuss-phase.md has 3 logging events with logger.X() example code sections.
research-phase.md has 3 logging events with logger.X() example code sections.
map-codebase.md has 4 logging events with logger.X() example code sections.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix simple orchestrator logging format (5 events)</name>
  <files>commands/gsd/resume-work.md, commands/gsd/complete-milestone.md</files>
  <action>
Update the `<logging>` section in both files to match the Phase 3 hybrid format.

Apply the same format transformation as Task 1.

**resume-work.md events to fix (2):**
1. Resume Start (INFO) - logger.info()
2. Resume Complete (INFO) - logger.info()

**complete-milestone.md events to fix (3):**
1. Milestone Start (INFO) - logger.info()
2. Archive Creation (DEBUG) - logger.debug()
3. Milestone Complete (INFO) - logger.info()

Keep the same event names, levels, and context fields - only change the FORMAT to include logger.X() wrapper.
  </action>
  <verify>
grep -c "logger\\.info\\|logger\\.debug" commands/gsd/resume-work.md  # Should be >= 2
grep -c "logger\\.info\\|logger\\.debug" commands/gsd/complete-milestone.md  # Should be >= 3
grep -c "Example code:" commands/gsd/resume-work.md  # Should be 2
grep -c "Example code:" commands/gsd/complete-milestone.md  # Should be 3
  </verify>
  <done>
resume-work.md has 2 logging events with logger.X() example code sections.
complete-milestone.md has 3 logging events with logger.X() example code sections.
All 27 events across 7 files now have consistent hybrid format with logger.X() wrappers.
  </done>
</task>

</tasks>

<verification>
1. All 7 files contain `logger.info(` or `logger.debug(` calls in their logging sections
2. All files have "Example code:" sections for each logging event
3. Format matches execute-phase.md and plan-phase.md (verified files from plan 05-01)
4. No regressions - existing context fields and event names preserved
</verification>

<success_criteria>
- 27 logging events across 7 files have hybrid format with logger.X() wrapper examples
- grep "logger\\.(info|debug)\\(" returns matches for all 7 workflow files
- Format consistent with Phase 3 standard and plan 05-01 implementations
- Phase 5 verification should pass (5/5 truths) after this fix
</success_criteria>

<output>
After completion, create `.planning/phases/05-workflow-integration/05-03-SUMMARY.md`
</output>
