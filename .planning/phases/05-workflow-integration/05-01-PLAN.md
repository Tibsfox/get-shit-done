---
phase: 05-workflow-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/execute-phase.md
  - commands/gsd/plan-phase.md
autonomous: true

must_haves:
  truths:
    - "execute-phase.md has logging section with wave start/complete, aggregation patterns"
    - "plan-phase.md has logging section with research/planning orchestration patterns"
    - "Logging specs follow hybrid format (prose + JavaScript examples)"
  artifacts:
    - path: "commands/gsd/execute-phase.md"
      provides: "Orchestrator logging specs for wave execution"
      contains: "<logging>"
    - path: "commands/gsd/plan-phase.md"
      provides: "Orchestrator logging specs for research and planning"
      contains: "<logging>"
  key_links:
    - from: "commands/gsd/execute-phase.md"
      to: "lib/logger.js"
      via: "logging spec references"
      pattern: "logger\\.(info|debug|warn)"
    - from: "commands/gsd/plan-phase.md"
      to: "lib/logger.js"
      via: "logging spec references"
      pattern: "logger\\.(info|debug|warn)"
---

<objective>
Add logging specifications to core workflow orchestrators (execute-phase.md, plan-phase.md).

Purpose: These orchestrators control phase execution and planning - they need logging for wave-based parallel execution correlation, subagent spawning, and aggregation patterns. This is the foundation for workflow observability.

Output: Two workflow files updated with `<logging>` sections containing orchestrator-specific event specifications following the hybrid format from Phase 3.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-workflow-integration/05-RESEARCH.md

Key patterns from research:
- Orchestrators log COORDINATION (wave start/complete, aggregation), not execution (that's agent responsibility)
- INFO level for workflow milestones (wave start/complete, phase complete)
- DEBUG level for coordination operations (plan discovery, wave grouping)
- Include wave context in all logs for parallel execution correlation
- Log outcome AFTER verification, not before

Key patterns from Phase 3 agent specs:
- Hybrid format: prose description + JavaScript code examples
- Section headers: "### N. Event Name (LEVEL level)"
- Context fields documented with bullet lists
- Example code shows exact logger.X() call with context object
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add logging section to execute-phase.md</name>
  <files>commands/gsd/execute-phase.md</files>
  <action>
Add a `<logging>` section before `<success_criteria>` in execute-phase.md with orchestrator logging specifications.

Include these events (following hybrid format from gsd-executor.md):

### 1. Wave Start (INFO level)
- Log before spawning parallel agents
- Context: phase, wave, plans (array of plan IDs), plan_count

### 2. Subagent Spawn (DEBUG level)
- Log when spawning each executor
- Context: agent_type, plan, wave, phase, model

### 3. Wave Complete (INFO level)
- Log AFTER verifying all agents completed
- Context: phase, wave, duration_ms, plans_completed, outcomes {success, failure, partial}

### 4. Phase Complete (INFO level)
- Log after all waves complete
- Context: phase, plans_total, plans_completed, waves_executed, total_duration_ms, outcomes, verification_status

### 5. Verification Spawn (DEBUG level)
- Log when spawning verifier
- Context: agent_type ("gsd-verifier"), phase, model

### 6. Orchestrator Corrections (DEBUG level)
- Log when orchestrator commits corrections (step 6)
- Context: phase, files_corrected, commit_hash

Each event should have:
- Message format description
- Context fields as bullet list
- JavaScript code example using logger.info/debug pattern

Reference the research patterns for exact field names and aggregation approach.
  </action>
  <verify>grep -c "logger\\.info\\|logger\\.debug" commands/gsd/execute-phase.md should return 6+ (one per event example)</verify>
  <done>execute-phase.md has `<logging>` section with 6 orchestrator events, wave correlation context, and JavaScript examples</done>
</task>

<task type="auto">
  <name>Task 2: Add logging section to plan-phase.md</name>
  <files>commands/gsd/plan-phase.md</files>
  <action>
Add a `<logging>` section before `<success_criteria>` in plan-phase.md with orchestrator logging specifications.

Include these events (following hybrid format):

### 1. Research Check (DEBUG level)
- Log decision about whether research needed
- Context: phase, research_exists, force_research, decision (spawn_researcher | skip_to_planning)

### 2. Research Start (INFO level)
- Log when spawning researcher
- Context: phase, mode (forced | automatic)

### 3. Subagent Spawn (DEBUG level)
- Log when spawning researcher or planner
- Context: agent_type, phase, model

### 4. Research Complete (INFO level)
- Log when researcher returns
- Context: phase, duration_ms, outcome, confidence

### 5. Planning Start (INFO level)
- Log when spawning planner
- Context: phase, mode (standard | gap_closure | revision), research_available

### 6. Planning Complete (INFO level)
- Log when planner returns successfully
- Context: phase, duration_ms, plans_created, waves_count

### 7. Verification Start (DEBUG level)
- Log when spawning plan-checker
- Context: agent_type ("gsd-plan-checker"), phase, iteration

### 8. Verification Result (INFO level)
- Log checker outcome
- Context: phase, iteration, status (passed | issues_found), issues_count

### 9. Revision Loop (DEBUG level)
- Log revision iteration
- Context: phase, iteration, issues_to_address

Each event should have message format, context fields, and JavaScript example.
  </action>
  <verify>grep -c "logger\\.info\\|logger\\.debug" commands/gsd/plan-phase.md should return 9+ (one per event example)</verify>
  <done>plan-phase.md has `<logging>` section with 9 orchestrator events covering research through verification loop</done>
</task>

<task type="auto">
  <name>Task 3: Commit core workflow logging specs</name>
  <files>commands/gsd/execute-phase.md, commands/gsd/plan-phase.md</files>
  <action>
Stage and commit the updated workflow files:

```bash
git add commands/gsd/execute-phase.md commands/gsd/plan-phase.md
git commit -m "docs(05-01): add logging specs to core workflow orchestrators

- execute-phase.md: wave start/complete, subagent spawn, phase aggregation
- plan-phase.md: research/planning lifecycle, checker iteration loop

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```
  </action>
  <verify>git log -1 --oneline shows commit with "docs(05-01)" prefix</verify>
  <done>Core workflow logging specs committed with descriptive message</done>
</task>

</tasks>

<verification>
After all tasks:
1. `grep -l "<logging>" commands/gsd/execute-phase.md commands/gsd/plan-phase.md` returns both files
2. Each logging section follows hybrid format (prose + code examples)
3. Wave context included in execute-phase events for parallel correlation
4. Revision loop iteration tracked in plan-phase events
5. Commit exists in git log
</verification>

<success_criteria>
- execute-phase.md has `<logging>` section with wave-based execution events
- plan-phase.md has `<logging>` section with research/planning orchestration events
- All events use consistent naming from research (Wave start, Phase complete, etc.)
- JavaScript examples show exact logger.level() calls with context objects
- Changes committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/05-workflow-integration/05-01-SUMMARY.md`
</output>
