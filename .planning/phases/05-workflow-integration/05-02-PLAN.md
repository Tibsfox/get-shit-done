---
phase: 05-workflow-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/verify-work.md
  - commands/gsd/discuss-phase.md
  - commands/gsd/debug.md
  - commands/gsd/research-phase.md
  - commands/gsd/resume-work.md
  - commands/gsd/map-codebase.md
  - commands/gsd/complete-milestone.md
autonomous: true

must_haves:
  truths:
    - "verify-work.md has logging for UAT session lifecycle and parallel debug agent spawning"
    - "discuss-phase.md has logging for context gathering operations"
    - "debug.md has logging for investigation session lifecycle"
    - "Remaining orchestrators have logging appropriate to their complexity"
  artifacts:
    - path: "commands/gsd/verify-work.md"
      provides: "UAT session and debug agent orchestration logging"
      contains: "<logging>"
    - path: "commands/gsd/debug.md"
      provides: "Debug session lifecycle logging"
      contains: "<logging>"
    - path: "commands/gsd/discuss-phase.md"
      provides: "Context gathering logging"
      contains: "<logging>"
    - path: "commands/gsd/research-phase.md"
      provides: "Standalone research logging"
      contains: "<logging>"
    - path: "commands/gsd/resume-work.md"
      provides: "Session resume logging"
      contains: "<logging>"
    - path: "commands/gsd/map-codebase.md"
      provides: "Codebase mapping logging"
      contains: "<logging>"
    - path: "commands/gsd/complete-milestone.md"
      provides: "Milestone completion logging"
      contains: "<logging>"
  key_links:
    - from: "commands/gsd/verify-work.md"
      to: "lib/logger.js"
      via: "logging spec references"
      pattern: "logger\\.(info|debug|warn)"
    - from: "commands/gsd/debug.md"
      to: "lib/logger.js"
      via: "logging spec references"
      pattern: "logger\\.(info|debug|warn)"
---

<objective>
Add logging specifications to remaining workflow orchestrators (verify-work, discuss-phase, debug, research-phase, resume-work, map-codebase, complete-milestone).

Purpose: Complete workflow observability coverage. These workflows handle UAT sessions, debugging, context gathering, and milestone management - all need appropriate logging for traceability and debugging.

Output: Seven workflow files updated with `<logging>` sections appropriate to their complexity and orchestration patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-workflow-integration/05-RESEARCH.md

Key patterns from research:
- UAT sessions: Log test presentation, results, checkpoint frequency
- Debug sessions: Log symptom gathering, investigation lifecycle, root cause findings
- Simpler orchestrators: Focus on start/complete events, subagent spawning
- Parallel agent spawning (map-codebase) needs correlation similar to wave execution

Complexity tiers:
1. HIGH (full logging): verify-work.md, debug.md (long-running, multi-checkpoint)
2. MEDIUM: discuss-phase.md, research-phase.md, map-codebase.md (subagent spawning)
3. LOW: resume-work.md, complete-milestone.md (simpler orchestration)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add logging to verify-work.md and debug.md (complex orchestrators)</name>
  <files>commands/gsd/verify-work.md, commands/gsd/debug.md</files>
  <action>
Add `<logging>` sections to both files before `<success_criteria>`.

**For verify-work.md (UAT orchestrator):**

### 1. UAT Session Start (INFO level)
- Context: phase, tests_total, session_id, resume (boolean)

### 2. Test Present (DEBUG level)
- Context: session_id, test_number, test_total, test_id, expected_behavior

### 3. Test Result (INFO level)
- Context: session_id, test_number, test_id, status (pass|fail), severity (if fail), issue (if fail)

### 4. UAT Checkpoint (DEBUG level)
- Context: session_id, tests_completed, tests_remaining

### 5. Debug Agent Spawn (DEBUG level)
- Context: agent_type ("gsd-debugger"), session_id, issue_id, model

### 6. Planner Spawn (DEBUG level)
- Context: agent_type ("gsd-planner"), phase, mode ("gap_closure")

### 7. UAT Session Complete (INFO level)
- Context: phase, session_id, duration_ms, tests_total, tests_passed, tests_failed, critical_issues

**For debug.md (investigation orchestrator):**

### 1. Debug Session Start (INFO level)
- Context: session_id, issue_slug, trigger (issue description), active_sessions_count

### 2. Symptom Gathering (DEBUG level)
- Context: session_id, symptom_type, response_received

### 3. Debugger Spawn (DEBUG level)
- Context: agent_type ("gsd-debugger"), session_id, model, mode (initial|continuation)

### 4. Checkpoint Handling (INFO level)
- Context: session_id, checkpoint_type, awaiting

### 5. Investigation Outcome (INFO level)
- Context: session_id, outcome (root_cause_found|inconclusive), duration_ms, hypotheses_tested

Each event: message format, context fields, JavaScript example.
  </action>
  <verify>grep -c "<logging>" commands/gsd/verify-work.md commands/gsd/debug.md shows 2 files have logging sections</verify>
  <done>verify-work.md and debug.md have comprehensive logging sections for session lifecycle</done>
</task>

<task type="auto">
  <name>Task 2: Add logging to medium complexity orchestrators</name>
  <files>commands/gsd/discuss-phase.md, commands/gsd/research-phase.md, commands/gsd/map-codebase.md</files>
  <action>
Add `<logging>` sections to all three files before `<success_criteria>`.

**For discuss-phase.md:**

### 1. Discussion Start (INFO level)
- Context: phase, gray_areas_count, context_exists (boolean)

### 2. Area Deep-Dive (DEBUG level)
- Context: phase, area_name, questions_asked, user_satisfied

### 3. Context Creation (INFO level)
- Context: phase, areas_discussed, duration_ms, decisions_captured

**For research-phase.md:**

### 1. Research Start (INFO level)
- Context: phase, mode (ecosystem|feasibility|implementation|comparison), existing_research (boolean)

### 2. Researcher Spawn (DEBUG level)
- Context: agent_type ("gsd-phase-researcher"), phase, model

### 3. Research Complete (INFO level)
- Context: phase, duration_ms, outcome, confidence

**For map-codebase.md (parallel agent spawning):**

### 1. Mapping Start (INFO level)
- Context: focus_area (if specified), existing_map (boolean)

### 2. Mapper Spawn (DEBUG level)
- Context: agent_id, agent_type ("gsd-codebase-mapper"), focus (tech|arch|quality|concerns), model

### 3. Mapper Complete (DEBUG level)
- Context: agent_id, focus, documents_written, duration_ms

### 4. Mapping Complete (INFO level)
- Context: agents_total, agents_completed, documents_total, total_duration_ms
  </action>
  <verify>grep -c "<logging>" commands/gsd/discuss-phase.md commands/gsd/research-phase.md commands/gsd/map-codebase.md shows 3 files</verify>
  <done>Medium complexity orchestrators have logging sections appropriate to their subagent spawning patterns</done>
</task>

<task type="auto">
  <name>Task 3: Add logging to simple orchestrators</name>
  <files>commands/gsd/resume-work.md, commands/gsd/complete-milestone.md</files>
  <action>
Add compact `<logging>` sections to both files before `<success_criteria>`.

**For resume-work.md:**

### 1. Resume Start (INFO level)
- Context: project_path, state_exists (boolean), checkpoints_found, incomplete_work_found

### 2. Resume Complete (INFO level)
- Context: routed_to (command suggested), context_loaded (boolean)

**For complete-milestone.md:**

### 1. Milestone Completion Start (INFO level)
- Context: version, phases_count, audit_status (passed|gaps_found|missing)

### 2. Archive Creation (DEBUG level)
- Context: version, archive_path, stats (phases, plans, commits)

### 3. Milestone Complete (INFO level)
- Context: version, tag_created (boolean), duration_ms

These are simpler orchestrators so only need 2-3 events each.
  </action>
  <verify>grep -c "<logging>" commands/gsd/resume-work.md commands/gsd/complete-milestone.md shows 2 files</verify>
  <done>Simple orchestrators have concise logging sections covering start/complete</done>
</task>

<task type="auto">
  <name>Task 4: Commit remaining workflow logging specs</name>
  <files>commands/gsd/verify-work.md, commands/gsd/discuss-phase.md, commands/gsd/debug.md, commands/gsd/research-phase.md, commands/gsd/resume-work.md, commands/gsd/map-codebase.md, commands/gsd/complete-milestone.md</files>
  <action>
Stage and commit all updated workflow files:

```bash
git add commands/gsd/verify-work.md commands/gsd/discuss-phase.md commands/gsd/debug.md commands/gsd/research-phase.md commands/gsd/resume-work.md commands/gsd/map-codebase.md commands/gsd/complete-milestone.md
git commit -m "docs(05-02): add logging specs to remaining workflow orchestrators

- verify-work.md: UAT session lifecycle, parallel debug spawning
- debug.md: investigation session, hypothesis tracking
- discuss-phase.md: context gathering, deep-dive logging
- research-phase.md: standalone research logging
- map-codebase.md: parallel mapper agent correlation
- resume-work.md: session resume logging
- complete-milestone.md: milestone archive logging

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```
  </action>
  <verify>git log -1 --oneline shows commit with "docs(05-02)" prefix</verify>
  <done>All remaining workflow logging specs committed</done>
</task>

</tasks>

<verification>
After all tasks:
1. `grep -l "<logging>" commands/gsd/*.md | wc -l` returns at least 9 (7 from this plan + 2 from plan 01)
2. Complex orchestrators (verify-work, debug) have 5+ events
3. Medium orchestrators (discuss-phase, research-phase, map-codebase) have 3-4 events
4. Simple orchestrators (resume-work, complete-milestone) have 2-3 events
5. All commits exist in git log
</verification>

<success_criteria>
- verify-work.md has logging for UAT session lifecycle and debug agent spawning
- debug.md has logging for investigation session and checkpoint handling
- discuss-phase.md has logging for context gathering operations
- research-phase.md, map-codebase.md, resume-work.md, complete-milestone.md all have appropriate logging
- Logging complexity matches orchestrator complexity (not over-logging simple commands)
- All changes committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/05-workflow-integration/05-02-SUMMARY.md`
</output>
