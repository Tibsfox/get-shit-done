---
phase: 06-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/settings.md
autonomous: true

must_haves:
  truths:
    - "Users can see current logging level in /gsd:settings output"
    - "Users can see syslog enabled status in /gsd:settings output"
    - "Settings display explains how to change logging configuration"
  artifacts:
    - path: "commands/gsd/settings.md"
      provides: "Logging settings display section"
      contains: "Logging"
  key_links:
    - from: "commands/gsd/settings.md"
      to: "lib/logger-config.js"
      via: "displays config values from logging section"
      pattern: "logging|level|syslog"
---

<objective>
Integrate logging configuration display into /gsd:settings command.

Purpose: Users can see their current logging settings alongside other GSD configuration.
Output: Updated settings.md with logging section in display output.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Current settings implementation
@commands/gsd/settings.md

# Logging config reference
@lib/logger-config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add logging display to /gsd:settings</name>
  <files>commands/gsd/settings.md</files>
  <action>
Update commands/gsd/settings.md to display current logging configuration as a read-only section.

**Changes to make:**

1. **Update description** (line 3):
   Change to: "Configure GSD workflow toggles, model profile, and view logging settings"

2. **Update Read Current Config section** (after step 2):
   Add parsing for logging config:
   ```
   - `logging.level` — current log level (default: 3/INFO)
   - `logging.syslog.enabled` — whether syslog output is active (default: true)
   ```

3. **Update Confirm Changes section** (step 5):
   Add logging display AFTER the existing settings table and BEFORE "Quick commands":

```
**Logging (read-only):**

| Setting        | Value |
|----------------|-------|
| Log Level      | {level_name} ({level_number}) |
| Syslog Output  | {Enabled/Disabled} |

To change logging settings, edit `.planning/config.json`:
```json
{
  "logging": {
    "level": "debug",
    "syslog": { "enabled": true }
  }
}
```
Or set environment variable: `GSD_LOG_LEVEL=4`

See `references/logging.md` for full configuration guide.
```

4. **Update Quick commands** section:
   Add at the end:
   ```
   - GSD_LOG_LEVEL=4 /gsd:... — enable DEBUG logging for one command
   ```

5. **Update success_criteria**:
   Add: `- [ ] Logging settings displayed (level, syslog status)`

The logging section is READ-ONLY (not interactive) because:
- Changing log level mid-session doesn't affect running processes
- Config file changes require process restart to take effect
- Environment variables are the preferred runtime override method

Match the existing table format (Setting | Value columns). Keep the section clearly marked as read-only with the header "**Logging (read-only):**".
  </action>
  <verify>
```bash
grep -q "Logging" commands/gsd/settings.md && grep -q "Log Level" commands/gsd/settings.md
```
  </verify>
  <done>
settings.md displays logging configuration (level, syslog status) in output with config change instructions.
  </done>
</task>

</tasks>

<verification>
1. `commands/gsd/settings.md` contains "Logging" section
2. Settings display includes Log Level and Syslog Output rows
3. Instructions reference `.planning/config.json` and `GSD_LOG_LEVEL`
4. Link to `references/logging.md` is present
</verification>

<success_criteria>
- [ ] Logging section added to settings display output
- [ ] Log Level shows both name and number (e.g., "INFO (3)")
- [ ] Syslog Output shows Enabled/Disabled
- [ ] Config change instructions included
- [ ] Reference to logging.md documentation
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-02-SUMMARY.md`
</output>
