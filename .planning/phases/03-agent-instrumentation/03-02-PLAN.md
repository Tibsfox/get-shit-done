---
phase: 03-agent-instrumentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-plan-checker.md
  - agents/gsd-phase-researcher.md
  - agents/gsd-project-researcher.md
  - agents/gsd-roadmapper.md
  - agents/gsd-codebase-mapper.md
  - agents/gsd-debugger.md
  - agents/gsd-integration-checker.md
  - agents/gsd-research-synthesizer.md
autonomous: true

must_haves:
  truths:
    - "All 8 remaining agent files have <logging> sections"
    - "Each agent has spawn and completion logging at INFO level"
    - "Agent-specific events are logged at appropriate levels"
    - "All logging specs use consistent format: prose description + example code"
  artifacts:
    - path: "agents/gsd-plan-checker.md"
      provides: "Plan checker logging specifications"
      contains: "<logging>"
    - path: "agents/gsd-phase-researcher.md"
      provides: "Phase researcher logging specifications"
      contains: "<logging>"
    - path: "agents/gsd-project-researcher.md"
      provides: "Project researcher logging specifications"
      contains: "<logging>"
    - path: "agents/gsd-roadmapper.md"
      provides: "Roadmapper logging specifications"
      contains: "<logging>"
    - path: "agents/gsd-codebase-mapper.md"
      provides: "Codebase mapper logging specifications"
      contains: "<logging>"
    - path: "agents/gsd-debugger.md"
      provides: "Debugger logging specifications"
      contains: "<logging>"
    - path: "agents/gsd-integration-checker.md"
      provides: "Integration checker logging specifications"
      contains: "<logging>"
    - path: "agents/gsd-research-synthesizer.md"
      provides: "Research synthesizer logging specifications"
      contains: "<logging>"
  key_links:
    - from: "agents/*.md"
      to: "logger.info|logger.debug"
      via: "code examples in <logging> section"
      pattern: "logger\\.(info|debug)"
---

<objective>
Add `<logging>` sections to the remaining 8 agent markdown files, completing Phase 3 agent instrumentation.

Purpose: These agents handle specialized operations (plan checking, research, debugging, etc.). Their logging specifications follow the pattern established by core agents (Plan 01) but are tailored to each agent's specific responsibilities.

Output: Eight updated agent markdown files with logging specifications that orchestrators can implement.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-instrumentation/03-CONTEXT.md
@.planning/phases/03-agent-instrumentation/03-RESEARCH.md
# Reference the core agents from Plan 01 for pattern consistency
@agents/gsd-executor.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add logging to checker and researcher agents</name>
  <files>
agents/gsd-plan-checker.md
agents/gsd-phase-researcher.md
agents/gsd-project-researcher.md
agents/gsd-research-synthesizer.md
  </files>
  <action>
Add `<logging>` sections to these 4 agents. Place each section before the agent's `<success_criteria>` section (or at end if no success_criteria exists).

**gsd-plan-checker.md** (checks plan quality):
- Agent Spawn (INFO): agent_id, agent_type, phase, plans_count
- Agent Completion (INFO): agent_id, outcome, duration_ms, issues_found, blockers, warnings
- Plan Analyzed (DEBUG): agent_id, plan_id, dimension, status, issues_count
- Issue Detected (INFO): agent_id, plan_id, dimension, severity, description
- Context Pressure (DEBUG/WARN): standard pattern

**gsd-phase-researcher.md** (researches phase implementation):
- Agent Spawn (INFO): agent_id, agent_type, phase, domain, has_context_md
- Agent Completion (INFO): agent_id, outcome, duration_ms, sections_produced, confidence
- Research Source (DEBUG): agent_id, source_type (context7/web/training), query, success
- Finding Documented (DEBUG): agent_id, section, confidence (HIGH/MEDIUM/LOW)
- Context Pressure (DEBUG/WARN): standard pattern

**gsd-project-researcher.md** (researches project domain):
- Agent Spawn (INFO): agent_id, agent_type, project_type, domain_keywords
- Agent Completion (INFO): agent_id, outcome, duration_ms, sections_produced, confidence
- Research Source (DEBUG): agent_id, source_type, query, success
- Context Pressure (DEBUG/WARN): standard pattern

**gsd-research-synthesizer.md** (synthesizes research findings):
- Agent Spawn (INFO): agent_id, agent_type, sources_count
- Agent Completion (INFO): agent_id, outcome, duration_ms, synthesis_quality
- Source Integrated (DEBUG): agent_id, source_path, sections_extracted
- Context Pressure (DEBUG/WARN): standard pattern

Use hybrid format: prose description + JavaScript code examples. Follow pattern from gsd-executor.md for consistency.
  </action>
  <verify>
for f in agents/gsd-plan-checker.md agents/gsd-phase-researcher.md agents/gsd-project-researcher.md agents/gsd-research-synthesizer.md; do
  grep -q "<logging>" "$f" && echo "$f: OK" || echo "$f: MISSING"
done
  </verify>
  <done>All 4 checker/researcher agents have <logging> sections with spawn, completion, and agent-specific events</done>
</task>

<task type="auto">
  <name>Task 2: Add logging to mapper and infrastructure agents</name>
  <files>
agents/gsd-roadmapper.md
agents/gsd-codebase-mapper.md
agents/gsd-integration-checker.md
  </files>
  <action>
Add `<logging>` sections to these 3 agents. Place each section before the agent's `<success_criteria>` section.

**gsd-roadmapper.md** (creates project roadmaps):
- Agent Spawn (INFO): agent_id, agent_type, project_type, requirements_count
- Agent Completion (INFO): agent_id, outcome, duration_ms, phases_created, total_plans
- Phase Defined (DEBUG): agent_id, phase_number, goal, requirements_mapped
- Dependency Resolved (DEBUG): agent_id, phase_number, depends_on
- Context Pressure (DEBUG/WARN): standard pattern

**gsd-codebase-mapper.md** (maps existing codebase):
- Agent Spawn (INFO): agent_id, agent_type, codebase_path, scan_scope
- Agent Completion (INFO): agent_id, outcome, duration_ms, files_scanned, documents_created
- Document Created (DEBUG): agent_id, document_type (STACK/ARCHITECTURE/etc), path
- Pattern Detected (DEBUG): agent_id, pattern_type, location, confidence
- Concern Flagged (INFO): agent_id, concern_type, severity, location, description
- Context Pressure (DEBUG/WARN): standard pattern

**gsd-integration-checker.md** (checks integration between phases):
- Agent Spawn (INFO): agent_id, agent_type, phases_to_check
- Agent Completion (INFO): agent_id, outcome, duration_ms, integrations_checked, issues_found
- Integration Checked (DEBUG): agent_id, from_phase, to_phase, status
- Issue Detected (INFO): agent_id, integration_point, severity, description
- Context Pressure (DEBUG/WARN): standard pattern

Use hybrid format: prose + code examples. These agents don't have checkpoints or deviations, so focus on their specialized domain events.
  </action>
  <verify>
for f in agents/gsd-roadmapper.md agents/gsd-codebase-mapper.md agents/gsd-integration-checker.md; do
  grep -q "<logging>" "$f" && echo "$f: OK" || echo "$f: MISSING"
done
  </verify>
  <done>All 3 mapper/infrastructure agents have <logging> sections with spawn, completion, and agent-specific events</done>
</task>

<task type="auto">
  <name>Task 3: Add logging to gsd-debugger.md</name>
  <files>agents/gsd-debugger.md</files>
  <action>
Add a `<logging>` section to gsd-debugger.md. The debugger has unique logging needs due to its investigative nature and checkpoint handling. Place section before `<success_criteria>`.

**gsd-debugger.md** (investigates bugs):
- Agent Spawn (INFO): agent_id, agent_type, mode (interactive/diagnose), bug_description_summary
- Agent Completion (INFO): agent_id, outcome, duration_ms, root_cause_found, fix_applied, verification_passed
- Investigation Phase (DEBUG): agent_id, phase (evidence_gathering/hypothesis_formation/testing/diagnosis), duration_ms
- Hypothesis Formed (DEBUG): agent_id, hypothesis_number, description, confidence
- Hypothesis Tested (DEBUG): agent_id, hypothesis_number, test_method, result (confirmed/rejected/inconclusive)
- Root Cause Identified (INFO): agent_id, root_cause, confidence, evidence_count
- Fix Applied (INFO): agent_id, fix_description, files_modified, commit_hash
- Checkpoint Pause (INFO): agent_id, checkpoint_type, reason, current_state
- Checkpoint Resume (INFO): agent_id, checkpoint_type, user_input_summary
- Context Pressure (DEBUG/WARN): standard pattern

The debugger's logging is more detailed because debugging sessions need clear audit trails for understanding what was tried and why.

Use hybrid format: prose + code examples. Include all checkpoint-related events since debugger frequently pauses for user input.
  </action>
  <verify>
grep -q "<logging>" agents/gsd-debugger.md && echo "OK" || echo "MISSING"
grep -c "Hypothesis" agents/gsd-debugger.md  # Should be 2+ (formed, tested)
grep -c "Root Cause" agents/gsd-debugger.md  # Should be 1+
  </verify>
  <done>gsd-debugger.md has <logging> section with spawn, completion, investigation phases, hypothesis tracking, fix application, and checkpoint events</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. All 8 remaining agents have logging sections:
   ```bash
   count=0
   for f in agents/gsd-plan-checker.md agents/gsd-phase-researcher.md agents/gsd-project-researcher.md agents/gsd-roadmapper.md agents/gsd-codebase-mapper.md agents/gsd-debugger.md agents/gsd-integration-checker.md agents/gsd-research-synthesizer.md; do
     grep -q "<logging>" "$f" && ((count++))
   done
   echo "$count/8 agents have logging sections"
   ```

2. Combined with Plan 01, all 11 agents are instrumented:
   ```bash
   grep -l "<logging>" agents/gsd-*.md | wc -l
   ```
   Should return 11.

3. Consistent format across all files:
   - Each file has "Agent spawn" and "Agent completion"
   - Code examples use structured context objects
   - Log levels follow CONTEXT.md decisions
</verification>

<success_criteria>
- [ ] All 8 remaining agent files have <logging> sections
- [ ] Each agent has spawn (INFO) and completion (INFO) events
- [ ] Each agent has relevant domain-specific events at appropriate levels
- [ ] gsd-debugger.md includes hypothesis tracking and checkpoint events
- [ ] All sections use hybrid format (prose + code examples)
- [ ] Combined with Plan 01, all 11 agents are instrumented
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-instrumentation/03-02-SUMMARY.md`
</output>
