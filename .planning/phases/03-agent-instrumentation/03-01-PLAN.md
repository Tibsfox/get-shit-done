---
phase: 03-agent-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-executor.md
  - agents/gsd-verifier.md
  - agents/gsd-planner.md
autonomous: true

must_haves:
  truths:
    - "gsd-executor.md contains a <logging> section with spawn, completion, checkpoint, and deviation logging specs"
    - "gsd-verifier.md contains a <logging> section with spawn, completion, and verification-specific logging specs"
    - "gsd-planner.md contains a <logging> section with spawn, completion, and planning-specific logging specs"
    - "All logging specs use consistent format: prose description + example code"
    - "Log levels follow CONTEXT.md decisions: spawn=INFO, completion=INFO, checkpoints=INFO"
  artifacts:
    - path: "agents/gsd-executor.md"
      provides: "Executor logging specifications"
      contains: "<logging>"
    - path: "agents/gsd-verifier.md"
      provides: "Verifier logging specifications"
      contains: "<logging>"
    - path: "agents/gsd-planner.md"
      provides: "Planner logging specifications"
      contains: "<logging>"
  key_links:
    - from: "agents/gsd-executor.md"
      to: "logger.info|logger.warn|logger.debug"
      via: "code examples in <logging> section"
      pattern: "logger\\.(info|warn|debug)"
---

<objective>
Add `<logging>` sections to the three core agent markdown files: gsd-executor.md, gsd-verifier.md, and gsd-planner.md.

Purpose: These core agents handle the most complex operations (execution with deviations, verification with gap detection, planning with research). Their logging specifications set the pattern for all other agents and cover the full range of logging events (spawn, completion, checkpoints, deviations, context pressure).

Output: Three updated agent markdown files with comprehensive logging specifications that orchestrators can implement.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-instrumentation/03-CONTEXT.md
@.planning/phases/03-agent-instrumentation/03-RESEARCH.md
@agents/gsd-executor.md
@agents/gsd-verifier.md
@agents/gsd-planner.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add logging section to gsd-executor.md</name>
  <files>agents/gsd-executor.md</files>
  <action>
Add a `<logging>` section to gsd-executor.md after the `<success_criteria>` section. The section should specify logging for:

**1. Agent Spawn (INFO level)**
- Message format: "Agent spawn: gsd-executor"
- Context: agent_id, agent_type, phase, plan, task, model, estimated_duration, parent_agent_id (if subagent), wave

**2. Agent Completion (INFO level)**
- Message format: "Agent completion: gsd-executor"
- Context: agent_id, outcome (success/failure/partial), duration_ms, tasks_completed, tasks_total, artifacts_created (SUMMARY.md, files), deviations_applied, checkpoints_hit, exit_status

**3. Checkpoint Pause (INFO level)**
- Message format: "Checkpoint pause"
- Context: agent_id, checkpoint_type, task_current, tasks_completed, tasks_total, awaiting, progress_snapshot

**4. Checkpoint Resume (INFO level)**
- Message format: "Checkpoint resume"
- Context: agent_id, checkpoint_type, user_response, validation_status

**5. Deviation Application (WARN level for Rule 1-3, INFO for Rule 4)**
- Message format: "Deviation applied: Rule {N}"
- Context: agent_id, rule_number, rule_type, task_current, trigger, action_taken, files_affected, in_scope, impact

**6. Context Pressure (DEBUG at 75%, WARN at 90%+)**
- Message format: "Context pressure warning" or "Context pressure critical"
- Context: agent_id, threshold, tokens_used, tokens_total, tokens_remaining, percent_used, rate_per_turn, estimated_turns_remaining

Use the hybrid format from CONTEXT.md: prose description followed by JavaScript code example showing exact logger method calls. Use template variables like `{agent_id}` in prose, actual variable names in code.

Place section BEFORE `</success_criteria>` closing tag so it appears near the end but within the main content.
  </action>
  <verify>
grep -c "<logging>" agents/gsd-executor.md returns 1
grep -c "Agent spawn" agents/gsd-executor.md returns at least 1
grep -c "Deviation applied" agents/gsd-executor.md returns at least 1
grep -c "Context pressure" agents/gsd-executor.md returns at least 1
  </verify>
  <done>gsd-executor.md contains a complete <logging> section with spawn, completion, checkpoint, deviation, and context pressure specifications</done>
</task>

<task type="auto">
  <name>Task 2: Add logging section to gsd-verifier.md</name>
  <files>agents/gsd-verifier.md</files>
  <action>
Add a `<logging>` section to gsd-verifier.md after the `<stub_detection_patterns>` section and before `<success_criteria>`. The section should specify logging for:

**1. Agent Spawn (INFO level)**
- Message format: "Agent spawn: gsd-verifier"
- Context: agent_id, agent_type, phase, is_re_verification, must_haves_count

**2. Agent Completion (INFO level)**
- Message format: "Agent completion: gsd-verifier"
- Context: agent_id, outcome, duration_ms, status (passed/gaps_found/human_needed), score, truths_verified, truths_total, gaps_count

**3. Verification Start (INFO level)**
- Message format: "Verification start"
- Context: agent_id, phase, plans_count, must_haves_count, mode (initial/re-verification)

**4. Artifact Check (DEBUG level)**
- Message format: "Artifact check: {path}"
- Context: agent_id, path, exists, substantive, wired, status, issue (if any)

**5. Key Link Verification (DEBUG level)**
- Message format: "Key link check: {from} -> {to}"
- Context: agent_id, from, to, via, status (wired/orphaned/partial/not_wired), details

**6. Gap Detection (INFO level)**
- Message format: "Gap detected: {truth}"
- Context: agent_id, truth, status, reason, artifacts_affected, missing_items

**7. Verification Outcome (INFO level)**
- Message format: "Verification outcome: {status}"
- Context: agent_id, status, score, gaps (array if any), human_verification_needed

**8. Context Pressure (DEBUG at 75%, WARN at 90%+)**
- Same pattern as executor

Use hybrid format: prose + code examples. Since verifier doesn't have checkpoints or deviations, omit those sections.
  </action>
  <verify>
grep -c "<logging>" agents/gsd-verifier.md returns 1
grep -c "Verification start" agents/gsd-verifier.md returns at least 1
grep -c "Artifact check" agents/gsd-verifier.md returns at least 1
grep -c "Gap detected" agents/gsd-verifier.md returns at least 1
  </verify>
  <done>gsd-verifier.md contains a complete <logging> section with spawn, completion, verification-specific events, and context pressure specifications</done>
</task>

<task type="auto">
  <name>Task 3: Add logging section to gsd-planner.md</name>
  <files>agents/gsd-planner.md</files>
  <action>
Add a `<logging>` section to gsd-planner.md after the `<structured_returns>` section and before `<success_criteria>`. The section should specify logging for:

**1. Agent Spawn (INFO level)**
- Message format: "Agent spawn: gsd-planner"
- Context: agent_id, agent_type, phase, mode (standard/gap_closure/revision), depth

**2. Agent Completion (INFO level)**
- Message format: "Agent completion: gsd-planner"
- Context: agent_id, outcome, duration_ms, plans_created, waves_count, total_tasks, mode

**3. Discovery Assessment (DEBUG level)**
- Message format: "Discovery level assessed"
- Context: agent_id, level (0-3), reason, action

**4. Plan Created (INFO level)**
- Message format: "Plan created: {plan_id}"
- Context: agent_id, plan_id, wave, tasks_count, files_modified, autonomous, has_checkpoints

**5. Wave Assignment (DEBUG level)**
- Message format: "Wave assignment complete"
- Context: agent_id, waves_count, parallel_plans, sequential_plans

**6. Must-Haves Derivation (DEBUG level)**
- Message format: "Must-haves derived"
- Context: agent_id, plan_id, truths_count, artifacts_count, key_links_count

**7. Gap Closure Mode (INFO level) - only when mode=gap_closure
- Message format: "Gap closure planning"
- Context: agent_id, gaps_count, source_file (VERIFICATION.md or UAT.md)

**8. Context Pressure (DEBUG at 75%, WARN at 90%+)**
- Same pattern as executor

Use hybrid format: prose + code examples. Planner doesn't have checkpoints or deviations in the same way as executor.
  </action>
  <verify>
grep -c "<logging>" agents/gsd-planner.md returns 1
grep -c "Discovery level" agents/gsd-planner.md returns at least 1
grep -c "Plan created" agents/gsd-planner.md returns at least 1
grep -c "Wave assignment" agents/gsd-planner.md returns at least 1
  </verify>
  <done>gsd-planner.md contains a complete <logging> section with spawn, completion, planning-specific events, and context pressure specifications</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. All three core agents have logging sections:
   ```bash
   for f in agents/gsd-executor.md agents/gsd-verifier.md agents/gsd-planner.md; do
     grep -l "<logging>" "$f"
   done
   ```
   Should output all three files.

2. Consistent format across files:
   - Each file has "Agent spawn" and "Agent completion" sections
   - Code examples use `logger.info()`, `logger.warn()`, `logger.debug()`
   - Template variables follow {variable_name} pattern in prose

3. Level assignments match CONTEXT.md decisions:
   - Spawn: INFO
   - Completion: INFO
   - Checkpoints: INFO
   - Deviations: WARN for Rule 1-3, INFO for Rule 4
   - Context pressure: DEBUG at thresholds, WARN at critical
</verification>

<success_criteria>
- [ ] gsd-executor.md has <logging> section with 6+ event types
- [ ] gsd-verifier.md has <logging> section with 8+ event types (verification-specific)
- [ ] gsd-planner.md has <logging> section with 7+ event types (planning-specific)
- [ ] All sections use hybrid format (prose + code examples)
- [ ] All sections use consistent log levels per CONTEXT.md decisions
- [ ] All code examples show structured context objects (not string interpolation)
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-instrumentation/03-01-SUMMARY.md`
</output>
