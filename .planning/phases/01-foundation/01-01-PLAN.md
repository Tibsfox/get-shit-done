---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/logger-config.js
autonomous: true

must_haves:
  truths:
    - "Configuration loads from environment variables with highest precedence"
    - "Configuration loads from .planning/config.json (project level)"
    - "Configuration loads from ~/.claude/gsd-config.json (global level)"
    - "Missing config files are handled gracefully with defaults"
    - "Log level can be set via GSD_LOG_LEVEL environment variable"
  artifacts:
    - path: "lib/logger-config.js"
      provides: "loadConfig() function with precedence merging"
      exports: ["loadConfig", "parseLevel", "LEVELS", "LEVEL_NAMES"]
      min_lines: 80
  key_links:
    - from: "lib/logger-config.js"
      to: "process.env"
      via: "GSD_LOG_LEVEL, GSD_LOG_SYSLOG environment variable reads"
      pattern: "process\\.env\\.GSD_LOG"
    - from: "lib/logger-config.js"
      to: "fs.readFileSync"
      via: "JSON config file loading"
      pattern: "readFileSync.*config\\.json"
---

<objective>
Create the configuration loading module for the GSD logging system that implements config precedence (env > project > global > defaults) and provides level parsing utilities.

Purpose: Enables logging behavior to be customized per-project or globally, with environment overrides for debugging sessions.
Output: lib/logger-config.js with loadConfig(), parseLevel(), and level constants.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib directory and logger-config.js with level constants</name>
  <files>lib/logger-config.js</files>
  <action>
Create lib/ directory if it doesn't exist.

Create lib/logger-config.js with:

1. Level constants:
   - LEVELS object: { OFF: 0, ERROR: 1, WARN: 2, INFO: 3, DEBUG: 4, TRACE: 5 }
   - LEVEL_NAMES array: ['OFF', 'ERROR', 'WARN', 'INFO', 'DEBUG', 'TRACE']
   - DEFAULTS object with sensible defaults (level: 3/INFO, syslog enabled, file disabled)

2. parseLevel(value) function:
   - Accept string names (case-insensitive): 'error', 'ERROR', 'Error' all return 1
   - Accept numeric strings: '3' returns 3
   - Accept numbers: 3 returns 3
   - Return null for invalid/undefined input (caller uses default)
   - Clamp to valid range 0-5

Use only Node.js built-in modules (fs, path, os). No external dependencies.
  </action>
  <verify>
Run: node -e "const c = require('./lib/logger-config.js'); console.log(c.LEVELS, c.parseLevel('error'), c.parseLevel(3), c.parseLevel('invalid'))"
Expected: { OFF: 0, ERROR: 1, ... }, 1, 3, null
  </verify>
  <done>LEVELS and LEVEL_NAMES exported, parseLevel correctly parses strings/numbers</done>
</task>

<task type="auto">
  <name>Task 2: Implement loadConfig with precedence merging</name>
  <files>lib/logger-config.js</files>
  <action>
Add to lib/logger-config.js:

1. safeReadJson(filepath) helper:
   - Return parsed JSON if file exists and is valid
   - Return null on any error (ENOENT, parse error, etc.)
   - Never throw - silent failure per project constraint

2. deepMerge(...objects) helper:
   - Recursively merge objects
   - Later objects override earlier
   - Handle undefined values (skip them in merge)

3. loadConfig() function:
   - Load defaults from DEFAULTS constant
   - Load global config: ~/.claude/gsd-config.json (look for .logging property)
   - Load project config: .planning/config.json (look for .logging property)
   - Build env config from:
     - GSD_LOG_LEVEL -> level (use parseLevel)
     - GSD_LOG_SYSLOG -> syslog.enabled (parse 'true'/'false'/1/0)
     - GSD_LOG_FACILITY -> syslog.facility
   - Merge with precedence: defaults < global < project < env
   - Return merged config object

Export: loadConfig, parseLevel, LEVELS, LEVEL_NAMES, DEFAULTS
  </action>
  <verify>
Run: GSD_LOG_LEVEL=debug node -e "const c = require('./lib/logger-config.js'); console.log(JSON.stringify(c.loadConfig(), null, 2))"
Expected: Config object with level: 4 (DEBUG), syslog.enabled: true, other defaults
  </verify>
  <done>loadConfig returns merged config with correct precedence, env vars override file config</done>
</task>

<task type="auto">
  <name>Task 3: Add parseBool utility and complete exports</name>
  <files>lib/logger-config.js</files>
  <action>
Add to lib/logger-config.js:

1. parseBool(value) function:
   - Return true for: 'true', '1', 1, true
   - Return false for: 'false', '0', 0, false
   - Return null for undefined/invalid (caller decides default)
   - Case-insensitive string comparison

2. Ensure all exports are at bottom of file:
   - module.exports = { loadConfig, parseLevel, parseBool, LEVELS, LEVEL_NAMES, DEFAULTS }

3. Add JSDoc comments to all exported functions for IDE support

4. Add header comment with module purpose and requirements covered:
   - CONFIG-01: Project config (.planning/config.json)
   - CONFIG-02: Global config (~/.claude/gsd-config.json)
   - CONFIG-03: Environment overrides
   - CONFIG-04: Sensible defaults
   - CONFIG-05: Config precedence

Verify no external dependencies - only require('node:fs'), require('node:path'), require('node:os')
  </action>
  <verify>
Run: node -e "const c = require('./lib/logger-config.js'); console.log(Object.keys(c), c.parseBool('true'), c.parseBool('0'), c.parseBool(undefined))"
Expected: ['loadConfig', 'parseLevel', 'parseBool', 'LEVELS', 'LEVEL_NAMES', 'DEFAULTS'], true, false, null
  </verify>
  <done>All utilities exported, JSDoc comments present, requirements CONFIG-01 through CONFIG-05 covered</done>
</task>

</tasks>

<verification>
1. lib/logger-config.js exists with all exports
2. node -e "require('./lib/logger-config.js')" runs without error
3. GSD_LOG_LEVEL=trace node -e "console.log(require('./lib/logger-config.js').loadConfig().level)" outputs 5
4. Config loads gracefully when no config files exist
</verification>

<success_criteria>
- loadConfig() returns valid config object with all required fields
- Environment variables override file configuration
- parseLevel handles all input types correctly
- No external dependencies (only node: prefixed built-ins)
- Silent failure on missing/invalid config files
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
